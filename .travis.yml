sudo: required
services:
  - docker

before_install:
  - docker build -t stephengrider/react-test -f ./client/Dockerfile.dev ./client

script:
  - docker run -e CI=true stephengrider/react-test npm test

after_success:
  - docker build -t xiaoqingyuan/multi-client ./client
  - docker build -t xiaoqingyuan/multi-nginx ./nginx
  - docker build -t xiaoqingyuan/multi-server ./server
  - docker build -t xiaoqingyuan/multi-worker ./worker
  # Log in to the docker CLI
  - echo "$DOCKER_PASSWORD" | docker login -u "$DOCKER_ID" --password-stdin
  # Take those images and push them to docker hub
  - docker push xiaoqingyuan/multi-client
  - docker push xiaoqingyuan/multi-nginx
  - docker push xiaoqingyuan/multi-server
  - docker push xiaoqingyuan/multi-worker
